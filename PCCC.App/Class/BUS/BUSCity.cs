using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using PCCC.App.Class;

namespace PCCC.App.Class.BUS
{
    public class BUSCity
    {
        //public bool ThemTP(City city)
        //{
        //    return new DAO.DAO_City().ThemTP(city.ID, city.Name, city.CCity);
        //}
        //public bool SuaTP(Model.City city)
        //{
        //    return new DAO.DAO_City().SuaTP(city.ID, city.Name, city.CCity);
        //}
        //public bool XoaTP(int id)
        //{
        //    return new DAO.DAO_City().XoaTP(id);
        //}
        //public IEnumerable<Model.City> LayDSTP()
        //{
        //    return new DAO.DAO_City().All();
        //}
    }
}
